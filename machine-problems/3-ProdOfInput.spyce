spyce() -> void {
    int a = 0, b = 0;

    say("Enter first number: ");
    a = tofloat(listen());

    say("Enter second number: ");
    b = tofloat(listen());

    say("Product: " + tostr(a * b));
    giveback void;
}